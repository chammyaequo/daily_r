print("Hello R!")
# The result will be "Hello R!" because print() outputs everything between ( and ) to the console.

cat("Hello R!\n")
# The result will be Hello R! because cat() only returns the string without quotes.

# In R, comments are created using the # symbol.