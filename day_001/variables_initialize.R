variable1 <- "Hello"
variable2 <- "R!"
cat(variable1, variable2, "\n")

# to initialize variables in R, you can use the assignment operator <- or =.
# The assignment operator <- is the preferred way to assign values to variables in R.
# The = operator can also be used, but it is more commonly used in function arguments.